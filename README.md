# Inventory prototype

Interactive usability-testing prototype built with React, TypeScript and Vite.
Changes made during testing are stored in browser sessionStorage. No backend or database is used.

## Development

```sh
npm ci
npm run dev -- --host 127.0.0.1
```

## Production build

```sh
npm run build
```

GitHub Pages serves the compiled website from the `gh-pages` branch.
